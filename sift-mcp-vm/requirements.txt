# Python dependencies for the Sift MCP server.
# Python 3.10+ required (SIFT 24.04 ships 3.12).
mcp[cli]>=1.2.0
pydantic>=2.6

# Optional, only needed if you want these capabilities and they aren't already
# present in the SIFT VM:
#   volatility3        # memory forensics (provides `vol`)
#   python-evtx        # provides evtx_dump.py for sift_parse_evtx
# Install via pipx so they get their own environment:
#   pipx install volatility3
#   pipx install python-evtx
